name: Rekhta Bot

"on":
  workflow_dispatch:
    inputs:
      max_items:
        description: "Max new items to add to PostQueue (empty = all new)"
        type: string
        required: false
        default: ""
      dry_run:
        description: "--dry-run"
        type: boolean
        required: true
        default: false
      debug:
        description: "Debug logging"
        type: boolean
        required: true
        default: false

jobs:
  run:
    uses: ./.github/workflows/dashboard.yml
    with:
      mode: rekhta
      max_items: ${{ github.event.inputs.max_items || '' }}
      dry_run: ${{ github.event.inputs.dry_run == 'true' }}
      headless: true
      debug: ${{ github.event.inputs.debug == 'true' }}
    secrets: inherit
